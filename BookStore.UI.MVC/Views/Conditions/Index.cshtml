@model IEnumerable<BookStore.DATA.EF.Models.Condition>

@{
    ViewData["Title"] = "Book Conditions";
}

<section class="container-fluid py-5">
    <div class="container">
        <div class="mx-auto text-center wow fadeIn" data-wow-delay="0.1s" style="max-width: 600px;">
            <h1 class="text-primary mb-3 bg-white border-radius 10"><span class="fw-light text-dark"><strong>Book</strong></span>Conditions</h1>
            @*             <h1 class="text-primary mb-3"><span class="fw-light text-dark">Books</span>For Sale</h1>
            *@        <p align="center">

                @if (User.IsInRole("Admin"))
                {
                    <span>
                        <a asp-action="Create" class="btn btn-info">Add New Condition</a>
                    </span>
                }

            </p>
        </div>
    </div>
</section>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ConditionDescription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ConditionDescription)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ConditionId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ConditionId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ConditionId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
